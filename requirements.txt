# Dependências das DAGs, instaladas na imagem derivada (docker/airflow/Dockerfile).
# Auditadas no CI por pip-audit (ADR-003). requests 2.33.0 corrige PYSEC-2026-1872/2275
# (que o CI pegou na imagem base 2.32.3 — o controle funcionando).
requests==2.33.0
psycopg2-binary==2.9.10
