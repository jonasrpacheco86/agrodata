# Screenshots

Prints para o README (capturados localmente com a stack no ar). Sugestão de 3 imagens:

1. `dashboard.png` — o dashboard do Metabase com os 3 indicadores (Fase 2): produção por ano/cultura,
   receita/ha e sazonalidade do preço. Monte por [`docs/dashboard-fase2.md`](../dashboard-fase2.md).
2. `airflow-dags.png` — a lista/grafo das 3 DAGs no Airflow (localhost:8080), mostrando a ingestão.
3. `mcp.png` — uma pergunta em linguagem natural respondida pelas ferramentas MCP (Claude Desktop ou
   MCP Inspector).

Depois de capturar, referencie no `README.md`, por exemplo:
`![Dashboard](docs/screenshots/dashboard.png)`.
